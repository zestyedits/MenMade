# Terms of Use — `/terms`

## What this page is for
The terms of use — same plain-language-summary pattern as Privacy. Static, indexable.

## How it looks today
Same shared `LegalShell` as Privacy (Navbar → centered `max-w-[1100px]` → header → `.legal-prose` → Footer). Header: title *"Terms of use"*, effective *"May 10, 2026"*, intro *"These are the rules of the road for using MenMade…"*. Sections: who we are, your account (18+), what you agree to do/not do, squads and content (users retain ownership, grant display license), subscriptions and payment, termination, disclaimers, limitation of liability, changes, governing law, contact. Placeholder-draft note. Links to `/contact` and `/settings`.

## Redesign goals
- Same readability upgrade as Privacy so the two legal pages are a consistent pair.
- Surface the genuinely user-relevant rules (account age, content ownership, cancellation) so they aren't buried in uniform prose.

## The prompt (paste into Lovable)

> Redesign our **Terms of Use** page for MenMade to match the **Privacy Policy** redesign exactly — they're
> a pair and must share the same shell (brand rules in `00-BRAND-SYSTEM.md`). Centered reading column, clear
> `<h2>/<h3>` hierarchy, plain-language summaries visually distinct from the binding legal text, sticky
> desktop table-of-contents that tracks scroll, collapsing on mobile. Header: title *"Terms of use"*,
> effective date, intro *"These are the rules of the road for using MenMade."* Make the user-relevant rules
> (18+ to hold an account, users retain ownership of their content, how cancellation works) scannable rather
> than buried. Dry MenMade tone in summaries, WCAG AA contrast on all body text, proper landmarks. Link to
> `/contact` and `/settings` where referenced.

# Landing / Home — `/`

> **STATUS: LOCKED REFERENCE — do not redesign.** This page is the visual source of truth.
> It's here so Lovable can study it and match its register on every other page. If you ask
> Lovable to touch anything, ask only for *parity polish*, never a rethink.

## What this page is for
The marketing homepage and primary conversion surface for logged-out visitors.

## How it looks today
Single long dark scroll, composed of locked sections in order:
- `ScrollProgress` bar pinned to top of viewport
- `Navbar` (sticky, blur-on-scroll)
- `Hero` — split/asymmetric, the "MADE" ember treatment
- `Impact` — typography-heavy stat callouts
- `SocialProof` — testimonials / case studies
- `FinalCta` — conversion block
- `Footer`

No forms or page-level state — everything lives in the section components. Film grain over the whole page.

## The prompt (only if asking for parity polish — otherwise skip)

> This is our locked landing page and the reference for the whole product's look. Do **not**
> redesign it. Study its rhythm — asymmetric hero, ember used at ~5–10%, mono kicker labels,
> generous negative space, film grain, dark ink-950 base — and treat it as the visual standard
> that every other screen I send you must match. If I point you at a specific section for polish,
> keep every brand rule in `00-BRAND-SYSTEM.md` and change nothing structural.
